class DataPaths {
    base = {
        users: 'users',
    }

    extension = {
        
    }

    globalBasePath = this.base.users
    globalExtensionPath = this.extension.set
}

export default new DataPaths()